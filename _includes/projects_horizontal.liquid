<div class="col mb-4">
  <div class="card h-100 hoverable">
    <div class="row no-gutters">
      {% if project.img %}
        <div class="col-md-6">
          {% include figure.liquid loading="eager" path=project.img sizes="(min-width: 768px) 156px, 50vw" alt="project thumbnail" class="card-img" %}
        </div>
      {% endif %}
      <div class="{% if project.img %}col-md-6{% else %}col-md-12{% endif %}">
        <div class="card-body">
          <h3 class="card-title">{{ project.title }}</h3>
          {% if project.timeline %}
            <p class="card-timeline"><i class="fa-regular fa-calendar"></i> {{ project.timeline }}</p>
          {% endif %}
          <p class="card-text">{{ project.description }}</p>
          <div class="project-links">
            {% if project.page_url %}
              <a href="{{ project.page_url }}" class="btn btn-project-page" target="_blank" rel="noopener noreferrer">
                <i class="fa-solid fa-globe"></i> Project Page
              </a>
            {% endif %}
            {% if project.source_url %}
              <a href="{{ project.source_url }}" class="btn btn-source-code" target="_blank" rel="noopener noreferrer">
                <i class="fa-brands fa-github"></i> Source Code
              </a>
            {% endif %}
          </div>
          {% if project.github %}
            <div class="row ml-1 mr-1 p-0">
              <div class="github-icon">
                <div class="icon" data-toggle="tooltip" title="Code Repository">
                  <a href="{{ project.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
                </div>
                {% if project.github_stars %}
                  <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                    <i class="fa-solid fa-star"></i>
                    <span id="{{ project.github_stars }}-stars"></span>
                  </span>
                {% endif %}
              </div>
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</div>
